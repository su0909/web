const assert  = require("assert")
const { exec:a, spawn } = {exec:1,spawn:2}

console.log(a,spawn)
// console.log(assert.deepEqual({a:1}, {a:1}));
// console.log(assert.deepEqual(assert.deepEqual({a:1,b:2}, {a:1,b:2})));
